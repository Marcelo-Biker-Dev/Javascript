let arr = ["A", "B", "C", "D", "E", "F"];
let item = "C";

var test = function(arr, item) {
    for (let i = 0; i < arr.length; i++) {
        if (item == arr[i]) {
            console.log(arr[i]);
        }
    }
};
test(arr, item);
